## Agent skills

### Issue tracker

事项和规格以 Markdown 文件形式存放在 `.scratch/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认的规范化分诊标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

使用单一上下文文档布局。详见 `docs/agents/domain.md`。
