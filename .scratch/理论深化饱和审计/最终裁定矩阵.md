# 三核心模块最终裁定矩阵

状态：`Pause / Not theoretically saturated`

说明：本矩阵只裁定当前公理、数据协议和八轮对抗审核下的数学成立性，不主张未经 Zotero 文献闭环验证的全球首创性。

| 主题 | 当前成立结论 | 被推翻或降级的结论 | 严格适用边界 | 可证伪预测/审计量 |
|---|---|---|---|---|
| 受保护参考 | 固定记录命令时，post-anchor 测量 token 对 protected rollout 的直接偏导为零 | “参考与 post-anchor 测量因果独立” | 仍保留 $y\to u\to\hat z$；clean anchor 是覆盖事件 | 固定 $u$ 扰动 $y$ 时直接 JVP 为零；允许控制器响应时总导数可非零 |
| 精确堆叠传播 | 段 Jacobian 积分给出精确解析递推；$\bm G_\nu$ 单位块下三角且可逆 | “在线 JVP 自动等于精确积分算子” | 安全排除需 $\|A-\widehat A\|\le\epsilon_A$ 的认证 enclosure | enclosure 覆盖率、区间宽度和未认证时的拒识率 |
| 锚点后滤波 | 全零化是锚点平移群的最大不变量；部分零化必须在 white space 选子空间并按实际 $L_b$ 定价残留 | “原残差欧氏奇异向量可直接投影”“部分零化等于完全不变” | $L_0=Q_w^\mathsf TW$；guard 使用 $I$；全部统计/阈值/签名共享同一 $L_b$ | $\|L_bG_0R_\eta^{1/2}\|$、guard-only 率、残留锚点能量 |
| 谱选择 | 奇异值阈值 $\tau$ 对应 Gram 投影 $\mathbf1_{(\tau^2,\infty)}(KK^\mathsf T)$；整簇处理重根 | 把 $\tau$ 直接与 $\sigma_i^2$ 比较 | 必须有谱隙；碰撞是独立 hybrid mode | 扫描 $\tau$ 时零化秩只在奇异值处跳变；错误量纲会产生反例中的空/多选子空间 |
| 白化信息边界 | 精确可逆预滤波加重白化只产生正交换基，不创造非中心度或类间角度 | “白化本身提高总体可分信息” | 有限样本 ridge/截断可产生非等变估计差异，但只能称正则化效应 | 比较总体/大样本白化前后非中心度和主角应不变 |
| 保留故障能量 | $\|Q_w^\mathsf TWg\|=\sqrt{\rho_W(g)}\|Wg\|$；共同临界值下幅值膨胀为 $\rho_W^{-1/2}$ | “白化后还要再乘协方差条件数” | 一般 matched branch 另含阈值比与投影增益 | 报告 $\rho_W$、实际阈值比、matched projection 三项分账 |
| 动态确定性阈值 | 联合 compact source set 的精确半径是 support function；block-column 和是可认证外包 | “block-column 和普遍比标量递推紧” | 共享路径变量不能边缘独立组合；算子误差必须联合传播 | aggregation-slack ratio、支路半径、零传播时 N.A. |
| 有限 episode 校准 | 冻结 score/state map 后，episode maximum 的 split-conformal 秩给有限时域家族误报界 | “逐点分位可保护后选停时/无限首报警” | $j_\delta=\lceil(n+1)(1-\delta)\rceil$；超出分辨率时 $q=+\infty$ | $n_{\rm cal}$、最小可实现 $\delta=1/(n+1)$、无限阈值模式 |
| 校准生命周期 | anchor gate、hysteresis、mode 与窗口状态转移在估计块冻结；最终分位不反馈 score path | “在校准块重放依赖最终分位的同一策略即可” | 防止 $q\to S(q)\to q$ 循环；检测与归因校准 episode 不得重用 | 状态轨迹对最终 $q$ 的不变性测试、split provenance |
| 正阈值几何 | $\Gamma_b=\max\{\epsilon_\Gamma,\gamma_b^{\rm anc}+\gamma_b^{\rm det}+\sigma_bq_\alpha\}>0$ 使 gauge 有定义 | “$\sigma_b>0$ 自动保证 $\Gamma_b>0$” | 正 floor 在校准前冻结；只会保守缩小告警事件 | 零残差/精确模型单元的 $\Gamma_b$ 与禁用率 |
| 故障可检测性 | 允许方向集上的受限增益 $\underline s_c>0$ 才支持 MDF；一般边界是正常/故障 tube 不相交 | “最小非零奇异值足以覆盖任意允许故障方向”“故障越大必然更易检” | H1 nuisance、mode 与路径事件独立声明；输入调度阈值可追平信号 | $M_c(A)$、$s_b-2\ell_b$、guard-only 与 suppressed-alarm 日志 |
| 物理可识别性 | raw record 只识别总测量侧/动力学侧注入；执行器需输入激励，传感器横向 JVP/控制器响应需外部覆盖，过程图需外部机理 | “only-normal 数据自动识别三类物理故障图” | 无外部结构时只能报告 learned-input-side、process-side 或 recorded-command-conditional 等价类 | 条件输入 Gramian 秩、横向扰动试验、过程先验宽度、controller-response 覆盖 |
| 完整诊断观测 | explanation graph 定义在 $(\mathsf m_{k^-},D_k)$ 及其全部可测输出上 | “仅 $(e,\Gamma,\mathrm{mask})$ 足以表征端到端决策” | 必须包含 $L_b,T_b,\Gamma_b$、operator enclosure、monitor state 与全部反事实 | 构造同 $e,\Gamma$ 不同 $L_b$ 的判决反例；检查状态序列哈希 |
| 集合值隔离 | 候选集包含所有可行故障支持与 Normal；非正常 singleton 才允许单标签 | “projection argmax/主角阈值可统一强制分类” | outer family Borel、嵌套、右闭；oracle refinement 单调，未决竞争者保留 | `Normal-compatible`/`Nonunique`/`Out-of-model`/equivalence-class 频率 |
| Normal 归因 | $\mathbb E_0^{\rm attr}=\mathbb A_0^{\rm det}\cup\overline{\mathbb E}_0^{\rm full}(q_\beta)$ | “用 detection score 的更大分位就是完整正常解释” | full-normal score 在独立 attribution-calibration episodes 上校准；$\beta$ 分辨率不足时永不排除 Normal | full-normal score episode maximum、正常排除 FWER、$q_\beta=+\infty$ 率 |
| 类间隔离边界 | 类别半径特例用 $\operatorname{dist}(v,\mathbb K_d)>r_c+r_d$；一般直接检查 explanation sets 不相交 | “Normal 与所有故障类统一使用因子 2” | 因子 2 只对两个完整单位对称 fault tube 精确；Normal 用完整观测集 | pairwise tube gap、半径分账、principal-angle 近似保守度 |
| 混合/顺序故障 | mixed support 由共同非线性映射与交互余项生成；真值是支持轨迹 $\bm c^\star$ | “单类签名线性相加即可”“episode 真类固定为一个 $c^\star$” | 同一物理 path 贯穿 onset、支持增删、模式与全部报告时刻 | support deletion test、mixed interaction budget、轨迹级 candidate coverage |
| 风险语义 | H0 的 $\beta$ 控制 episode 内错误排除 Normal；H1 的 $\delta+\epsilon$ 只在轨迹级 signature/nuisance 同时事件有预算时控制错误 singleton | “$\alpha$ 或 $\beta$ 是标签 PPV/FDR/后验置信度” | 逐窗口 H1 coverage 不控制 alarm-time 选择；无预算时只作条件 soundness | pointwise 与 selected-episode coverage 分开报告 |
| 噪声支持 | 支持相交否定 support-wise 鲁棒零错误；互相绝对连续/正密度重叠才推出几乎处处零错误不可能 | “拓扑全支撑单独推出所有概率零错误不可能” | 高概率 tube 只给 risk-limited attribution | raw-set overlap、密度重叠假设、tube truncation failure |

## 未闭合开放问题

1. 八轮上限已到，但第七、八轮仍产生新命题，尚无连续两轮 `NO_NEW_MATERIAL_CONCLUSION` 证据。
2. $\mathcal E_{\mathrm{sig},\bm c^\star}^{\mathrm{ep}}$ 与
   $\mathcal E_{\mathrm{nuis},\bm c^\star}^{\mathrm{ep}}$ 的物理概率预算尚无数据或外部机理验证。
3. certified segment-integral enclosure、full-normal outer oracle、单调 refinement cache 与五段数据切分尚未实现。
4. 正式 CSTR 冻结实验、表格数值、PDF 编译/目检仍未完成；当前只证明源文件静态一致性。
5. 由于本轮文献检索受 Semantic Scholar 限流且 Zotero 写入链不可用，创新性只能表述为当前稿内的结构性深化，不能宣称全球首创。
