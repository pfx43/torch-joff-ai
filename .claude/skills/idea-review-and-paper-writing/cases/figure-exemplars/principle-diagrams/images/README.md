# Authorized principle-diagram images

Store only authorized images listed in `../references.md`. Name each file
`<reference-id>-<short-description>.<ext>` and keep its rights status in the
inventory. An empty directory is normal when only metadata and links may be
stored.
