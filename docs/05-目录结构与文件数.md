# 目录结构与文件数

## 1. 文档目标

本文集中维护仓库目录职责、文件数量、Joff 核心文件地图和源码阅读顺序。运行时分层、调用链、接口与架构不变量见《[技术架构](./04-技术架构.md)》。

文件统计是时间点快照，不是长期常量。目录或文件发生实质变化时，应重新执行本文第 6 节的命令并更新日期和数字。

## 2. 统计口径

统计日期：**2026-07-29**。

“可版本化工作文件”按以下口径统计：

- 合并 Git 已跟踪文件和未跟踪但未被忽略的文件；
- 只计当前工作树中实际存在的普通文件；
- 排除 `.git/`、缓存、虚拟环境、`runs/`、构建产物、private 数据和其他 `.gitignore` 命中的文件；
- 包含 `.agents/`、`.claude/`、`.scratch/`、公开数据和当前未提交但未忽略的工作产物。

按该口径，仓库当前共有 **670 个可版本化工作文件**。

| 顶层位置 | 文件数 | 主要内容 |
|---|---:|---|
| `.agents/` | 137 | 面向通用代理工具的 skill 镜像 |
| `.claude/` | 137 | 面向 Claude Code 的 skill 镜像 |
| `.codegraph/` | 1 | CodeGraph 本地索引的版本化忽略规则；数据库和运行时文件不入库 |
| `.scratch/` | 25 | 事项、规格、推导账本和阶段性研究材料 |
| `configs/` | 5 | 通用示例与论文 smoke/development/frozen/TTS 阻塞态配置 |
| `datasets/` | 195 | 公开 manifest、数据集卡片和 OA 原始数据 |
| `docs/` | 14 | 项目文档、代理约定和归档材料 |
| `examples/` | 11 | quickstart、smoke、preset、paper smoke 和 study 示例 |
| `paper/` | 4 | 英文稿、BibTeX、PDF 和教师稿 |
| `src/` | 111 | 未被 Git 忽略的 Joff 源码文件 |
| `tests/` | 20 | pytest 测试文件 |
| 根目录独立文件 | 10 | 配置、入口文档、许可证和项目元数据 |
| **合计** | **670** | |

根目录 10 个独立文件为：`.gitattributes`、`.gitignore`、`.mcp.json`、`AGENTS.md`、`LICENSE`、`MANUSCRIPT_CONTEXT.md`、`pyproject.toml`、`README.md`、`README.zh-CN.md`、`skills-lock.json`。

## 3. 顶层目录结构

```text
paper_project/
|-- AGENTS.md                         # 自动化代理的唯一根级协作入口
|-- MANUSCRIPT_CONTEXT.md             # 英文论文项目级上下文
|-- README.md
|-- README.zh-CN.md
|-- pyproject.toml                    # Python 包、依赖、pytest 与 Ruff 配置
|
|-- src/joff/                         # Joff 运行时实现
|-- tests/                            # 单元、集成、真实数据迁移与 smoke 测试
|-- examples/                         # 可运行示例
|-- configs/                          # 示例配置，不是默认值权威来源
|
|-- datasets/
|   |-- manifest.public.yaml          # 公开数据清单
|   |-- cards/oa/                     # OA 数据集卡片
|   `-- raw/oa/                       # OA 原始数据
|
|-- paper/
|   |-- main.tex                      # 当前英文 IEEE 稿
|   |-- refs.bib                      # 当前参考文献
|   |-- main.pdf                      # 最近一次编译产物
|   `-- teacher_latest_manuscript/    # 教师提供的最新稿件
|
|-- docs/
|   |-- 01-文档导航.md                # 项目文档唯一入口
|   |-- 02-论文研究完整路线.md
|   |-- 03-当前工作记录.md
|   |-- 04-技术架构.md
|   |-- 05-目录结构与文件数.md
|   |-- agents/                       # 事项、分诊和领域文档约定
|   `-- 旧文档/                       # 被取代或归档的历史材料，含论文核心重构与写作计划
|
|-- .scratch/                         # 事项与研究过程材料
|-- .agents/skills/                   # skill 镜像 A
|-- .claude/skills/                   # skill 镜像 B
|-- .codegraph/                       # 本地代码索引；数据库、日志和 PID 被忽略
`-- runs/                             # 本地生成产物，Git 忽略
```

边界说明：

- `configs/example.yaml` 只是示例；运行时默认值在 `src/joff/core/defaults.py`。
- `datasets/raw/private/`、`datasets/cards/private/` 和 private 示例由 `.gitignore` 排除。
- `runs/`、checkpoint、追踪目录和 LaTeX 中间文件是生成产物，不计入文件总数。
- `.agents/skills/` 与 `.claude/skills/` 必须同步维护，不能只修改一侧。
- `docs/旧文档/` 只用于追溯，不作为当前论文或实现的权威规格。

## 4. Joff 核心源码

排除 `__pycache__` 后，`src/joff/` 当前物理存在 **112 个源码/类型标记文件**：111 个 `.py` 和 1 个 `py.typed`。

| 模块 | 文件数 | 责任 |
|---|---:|---|
| 包根目录 | 2 | 公共导出与类型标记 |
| `artifacts/` | 1 | 运行产物存储与 JSONL 事件日志 |
| `console/` | 5 | Rich/文本控制台、表格和进度 |
| `core/` | 10 | 严格配置、默认值、注册表、构建、设备、种子和 provenance |
| `data/` | 22 | source reader、adapter、schema、task、pipeline、DataModule、内存 loader 适配和论文五阶段协议 |
| `evaluation/` | 13 | 通用指标、P5--P9 受保护监视、算子、后滤波、动态阈值和结构化隔离 |
| `experiments/` | 17 | 通用实验/study、论文协议/基线和 P10 正常开发/冻结评价编排 |
| `layers/` | 5 | 激活函数、共享全连接、严格过去 attention 与 fuzzy Koopman |
| `models/` | 12 | 通用模型与论文 P4 Attention--Koopman--T--S 正常模型 |
| `plotting/` | 12 | 科研图尺寸、主题和各任务绘图器 |
| `tracking/` | 4 | 本地、组合和可选外部追踪适配器 |
| `training/` | 7 | Trainer、优化器、通用/P4 损失与诊断、回调和 checkpoint |
| `xai/` | 2 | Jacobian、批量 Jacobian 和 Hessian |
| **合计** | **112** | |

核心目录：

```text
src/joff/
|-- __init__.py
|-- py.typed
|-- artifacts/
|-- console/
|-- core/
|-- data/
|   |-- adapters/
|   |-- pipeline/
|   |-- schema/
|   `-- sources/
|-- evaluation/
|-- experiments/
|-- layers/
|-- models/
|-- plotting/
|-- tracking/
|-- training/
`-- xai/
```

### 4.1 核心文件地图

| 文件 | 主要职责 |
|---|---|
| `src/joff/__init__.py` | 顶层公共接口；导入必须保持安静 |
| `src/joff/artifacts/__init__.py` | `ArtifactStore` 与 `RunLogger` |
| `src/joff/core/config.py` | Pydantic 严格配置模型 |
| `src/joff/core/defaults.py` | 包级与模型级默认配置 |
| `src/joff/core/resolver.py` | 配置加载、深度合并、校验、溯源与哈希 |
| `src/joff/core/registry.py` | 名称、别名和构造器的显式注册表 |
| `src/joff/core/factory.py` | 按 `type` 构建模型和评估器 |
| `src/joff/data/datamodule.py` | 数据读取、选列、预处理、切分、窗口与 DataLoader 主入口 |
| `src/joff/data/in_memory.py` | 已准备内存 dataset 的 Trainer 数据层适配器 |
| `src/joff/data/paper_protocol.py` | 论文五阶段正常数据切分、拟合访问账本与冻结故障门禁 |
| `src/joff/data/tasks.py` | 分类、回归、重构、序列和 MPC 任务视图 |
| `src/joff/data/adapters/base.py` | `DatasetAdapter`、`CanonicalDataset` 与卡片适配 |
| `src/joff/data/adapters/registry.py` | 数据集 preset 注册与别名解析 |
| `src/joff/data/adapters/real_process.py` | CSTR、TE、TTS、NE、多相流和 WPT 等真实过程数据解析；TTS `fe` 变体固定 7/13 原始列宽、onset 和故障族 |
| `src/joff/data/sources/readers.py` | CSV、Excel、MAT、NPY 和 NPZ 通用读取 |
| `src/joff/data/pipeline/compose.py` | 流水线配置规范化与合并 |
| `src/joff/data/pipeline/split.py` | 顺序与动态分布切分 |
| `src/joff/data/pipeline/windowing.py` | 动态窗、序列和 MPC 窗口 |
| `src/joff/models/base.py` | `BaseModel`、batch 解析与统一损失约定 |
| `src/joff/models/koopman.py` | NKN 与 Koopman 潜在动力学 |
| `src/joff/models/attention.py` | 掩码注意力与 Attention 预测模型 |
| `src/joff/layers/causal_attention.py` | 有序严格过去编码、当前外生工况与可重放 channel mask |
| `src/joff/layers/fuzzy_koopman.py` | 可学习 T--S 规则、局部 Koopman 和完整解析 Jacobian |
| `src/joff/models/protected_koopman_ts.py` | P4 公开模型、训练视野采样和受保护自由 rollout |
| `src/joff/training/trainer.py` | epoch/batch 循环、评估和 checkpoint 协调 |
| `src/joff/training/checkpoint.py` | 模型、优化器、配置和随机状态保存恢复 |
| `src/joff/training/protected_losses.py` | P4 多视野分项损失与 fail-closed 正常验证诊断 |
| `src/joff/evaluation/fault_detection.py` | 正常拟合、T2/Q/SPE 与 FAR/MDR/FDR |
| `src/joff/evaluation/protected_reference.py` | P5 双支路、anchor 状态机、联合身份和可重放 trace |
| `src/joff/evaluation/residuals.py` | P5 同边界、同锚点的堆叠受保护潜变量残差 |
| `src/joff/evaluation/protected_operators.py` | P6 `G_nu/G_0`、响应传播、资源预算和联合认证 enclosure |
| `src/joff/evaluation/postfilter.py` | P7 white-space 后滤波、guard、matched branch 与严格重放 |
| `src/joff/evaluation/dynamic_threshold.py` | P8 输入/年龄包络、有限 episode 校准和动态阈值 |
| `src/joff/evaluation/explanations.py` | P9 完整部署观测、mask 重算证据和 explanation graph |
| `src/joff/evaluation/oracle.py` | P9 线性/未认证 outer oracle 与单调 refinement cache |
| `src/joff/evaluation/structured_isolation.py` | P9 full-normal 校准、集合值隔离和受控报告 |
| `src/joff/experiments/experiment.py` | 单次实验主调用链 |
| `src/joff/experiments/paper_baselines.py` | PCA、DAE、一步 MLP 正常基线与 checkpoint 重放 |
| `src/joff/experiments/paper_protocol.py` | 五阶段论文正常协议、静态 episode 校准与审计产物 |
| `src/joff/experiments/paper_entrypoints.py` | P10/P11 smoke/development/frozen 严格配置、TTS 物理布局与 CSTR 主协议锁 |
| `src/joff/experiments/paper_development.py` | P10 正常-only 训练、估计、校准和 development 产物生成；P11 次级运行前复验主 manifest 并执行阶段门禁 |
| `src/joff/experiments/paper_freeze.py` | P10 正常产物复核与冻结 manifest 构建 |
| `src/joff/experiments/frozen_cli.py` | P10 正式 preflight、一次性 claim 和 fail-closed 命令入口 |
| `src/joff/experiments/frozen_evaluation.py` | P10 manifest、claim、episode 结果、表图来源和完整性协议 |
| `src/joff/experiments/frozen_runtime.py` | P10 weights-only checkpoint evaluator 载入与运行绑定 |
| `src/joff/experiments/cstr_frozen_source.py` | P10 CSTR MAT header 审计与 manifest 绑定的八 episode 读取 |
| `src/joff/experiments/runner.py` | 多配置顺序运行与汇总 |
| `src/joff/experiments/study.py` | sweep、重复种子、约束与排行榜 |
| `src/joff/plotting/figure.py` | 论文图尺寸与 `FigureFactory` |
| `src/joff/tracking/optional.py` | TensorBoard、MLflow 和 W&B 可选适配器 |
| `src/joff/xai/jacobian.py` | Jacobian、批量 Jacobian 和 Hessian |

### 4.2 源码阅读顺序

1. `experiments/experiment.py`：理解一次通用实验如何串联所有模块。
2. `core/config.py`、`core/resolver.py`：理解配置来源、校验和覆盖优先级。
3. `data/datamodule.py` 与 `data/paper_protocol.py`：理解通用批次和论文五段边界。
4. `core/factory.py`、`models/base.py` 和目标模型：理解模型构建与输出约定。
5. `training/trainer.py`：理解训练、评估和 checkpoint。
6. `evaluation/`：理解预测如何转成 P5--P9 受保护检测与隔离对象。
7. `experiments/paper_entrypoints.py`、`paper_development.py`、`frozen_cli.py`：理解 P10 正常开发、冻结前检查、一次性正式运行，以及 P11 次级数据集执行门禁。
8. `experiments/study.py`：理解 sweep、重复实验和结果聚合。

## 5. 测试与示例分布

pytest 当前从 20 个文件收集 296 个测试：

| 测试文件 | 测试数 |
|---|---:|
| `test_cstr_protocol_repair.py` | 6 |
| `test_dynamic_threshold.py` | 14 |
| `test_examples_smoke.py` | 8 |
| `test_experiment_training_pipeline.py` | 7 |
| `test_frozen_cstr_evaluation.py` | 29 |
| `test_paper_data_protocol.py` | 11 |
| `test_paper_protocol_experiment.py` | 10 |
| `test_phase1_core.py` | 9 |
| `test_phase2_models_checkpoints.py` | 16 |
| `test_phase3_data_pipeline.py` | 32 |
| `test_phase4_phase5_workflows.py` | 24 |
| `test_protected_koopman_ts.py` | 12 |
| `test_protected_operators.py` | 22 |
| `test_protected_reference.py` | 10 |
| `test_real_dataset_migration.py` | 25 |
| `test_secondary_dataset_protocol.py` | 9 |
| `test_stacked_postfilter.py` | 18 |
| `test_structured_isolation.py` | 25 |
| `test_training_losses.py` | 5 |
| `test_xai_jacobian.py` | 4 |
| **合计** | **296** |

`examples/` 有 11 个文件，覆盖 MLP、DAE、NKN、故障检测、公开 preset、paper smoke、sweep、重复 study 和共用报告逻辑。示例的 smoke 数值只用于路径验证。

## 6. 重新统计

在仓库根目录执行以下 PowerShell：

```powershell
$files = git ls-files --cached --others --exclude-standard |
    Where-Object { Test-Path -LiteralPath $_ -PathType Leaf }

$files.Count

$files |
    ForEach-Object {
        $top = ($_ -split '[\\/]')[0]
        [PSCustomObject]@{ Top = $top; Path = $_ }
    } |
    Group-Object Top |
    Sort-Object Name |
    Select-Object Name, Count

Get-ChildItem src/joff -Recurse -File |
    Where-Object {
        $_.FullName -notmatch '[\\/]__pycache__[\\/]' -and
        $_.Extension -in '.py', '.typed'
    } |
    Measure-Object

python -m pytest --collect-only -q
```

计数更新后同时检查 `git status --short`，避免把用户正在移动但尚未纳入索引的文件误判为删除。

## 7. 已发现的清单风险

`.gitignore` 中未锚定的 `artifacts/` 规则会命中 `src/joff/artifacts/`。因此该目录的 `__init__.py` 虽然物理存在且被 `joff` 导入，却不在上表 670 个可版本化文件和 `src/` 的 111 个文件中；这正是“物理源码 112”与“可版本化源码 111”的差额。

在干净克隆或发布前，应单独修复忽略规则并确认 `src/joff/artifacts/__init__.py` 被版本控制。本次文档拆分不改动代码或忽略策略，只记录该风险。
