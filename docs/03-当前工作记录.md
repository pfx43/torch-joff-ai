# 当前工作记录

最近更新：2026-07-29

## 1. 当前阶段

历史方法设计已归档：《[论文方法完整设计](./旧文档/论文方法完整设计.tex)》（24 节，含对老师理论稿的逐条验证与修正、创新边界文献对比、代码模块落点）曾完成本地编译验证（36 页 PDF）；当前方案以重构计划、`MANUSCRIPT_CONTEXT.md` 和 `paper/main.tex` 为准。

2026-07-26 老师反馈：设计的核心内容不行，要求在三方面重点突破——堆叠残差的后滤波器设计、随输入和运行状态变化的动态阈值、三类故障的结构化隔离。已完成第一性原理诊断并制定《[论文核心重构与写作计划](./旧文档/论文核心重构与写作计划.md)》；随后完成英文稿和八轮理论深化，但没有达到连续两轮“无新增实质问题”，因此当前理论状态保持 **Pause / Not theoretically saturated**。

2026-07-29 P6 堆叠算子装配与联合认证状态已完成实现、相关验证和双轴审查整改。当前 **P1--P6 已完成，实施入口为 P7 white-space 堆叠残差后滤波**。完整论文方法仍未实现，仓库也没有真实 interval/verified-quadrature 认证后端。后续不再为每阶段另建规格或等待重复确认，直接以实施计划为规格执行 TDD、验证、代码审查和独立 Git 提交。

## 2. 已完成

- 已阅读老师提供的 7 页中文理论稿，并整理成《论文研究完整路线》。
- 已确认老师稿是一份理论蓝图，尚缺真实实验、数据协议和数值结果。
- 已梳理 Joff 的配置、数据、模型、训练、评估和实验编排架构。
- 已建立合并后的《技术架构与代码地图》。
- 已运行核心配置测试，`tests/test_phase1_core.py` 的 9 个测试通过。
- 已运行合成 CSTR 故障检测冒烟示例，确认基础训练、阈值、指标和产物保存路径可用。
- 已运行真实 CSTR preset 示例，确认数据适配和 DataLoader 基础路径可用。
- 完整测试套件曾在 120 秒检查窗口内未结束，因此目前不能声明全仓库测试全部通过。
- 2026-07-26：完成《论文方法完整设计》并本地编译验证。设计经多智能体验证（6 个定理独立重推导 + 4 条文献检索线 + 仓库逐文件审计），发现老师稿两处 critical 问题并给出修正：(a) 推论 1 的校准分数不可观测，改为校准可观测超额得分；(b) 隔离层残差与响应矩阵配对错位，改用堆叠受保护多步残差。文献边界确认三项贡献在收窄表述下成立（须对比 Wang 2005、US7587296、Zhang 2002 三个先行者）。
- 2026-07-26：完成闭环 CSTR 数据集深度审计——7 列 = u(Ci,Ti,Tci) + y(C,T,Tc,Qc)，normal 16814×7，fault01–08 各 1201×7、onset=200，故障 1–2 过程 / 3–5 执行器 / 6–8 传感器；发现适配器整段标故障（须逐行 onset 标签）、u/y 角色未进 schema、许可 to_verify 三个前置问题。
- 2026-07-26：装好本地 LaTeX 工具链（MiKTeX 26.5 + Strawberry Perl），现归档为 `docs/旧文档/编译PDF.ps1` 的脚本曾完成中文设计稿编译与质量检查。
- 2026-07-28：全面核对当前论文协议、Joff 运行架构、CSTR 适配器、训练/评估编排和现有测试，形成 P0-P11 代码实施计划。
- 2026-07-28：将“默认面向初学者进行详细、易懂解释”的沟通规则写入 `AGENTS.md`。
- 2026-07-28：规划基线验证完成。pytest 可收集 128 个测试；选取的 82 个测试中 76 个通过、6 个跳过；`python -m ruff check src tests examples` 通过。该结果不代表全套 128 个测试已全部运行通过。
- 2026-07-28：P1 修正 `cstr_closed_loop_fd`——schema 现显式区分 `control_input=(Ci,Ti,Tci)` 与 `measured_output=(C,T,Tc,Qc)`；fault01--08 在 `time<200` 保持正常标签、`time>=200` 使用对应故障编号；source summary 保存 episode、故障族、原始行号和源文件 SHA-256；DataModule sequence 保留官方 train/test segment 边界。
- 2026-07-28：P2 新增 `paper_protocol.py`——按原始时间轴形成 `train/estimate/detection_calibration/attribution_calibration/frozen_normal_test` 五个仅正常阶段，保留保守隔离带、窗口依赖、完整 episode、原始索引和确定性 hash；`FitAccessLedger` 限制拟合用途可见阶段，`PaperDataBundle` 在协议冻结和许可核实前拒绝返回真实故障范围，并可显式保存三份协议产物。
- 2026-07-28：P2 首轮双轴审查整改完成——移除可绕过账本的公开阶段数组读取；强制检测分位、归因分位和冻结正常诊断逐级冻结；校准窗口不得跨完整 episode；正式故障访问要求完整冻结账本、正常/故障来源 SHA-256 和受控许可枚举；根级 `AGENTS.md` 的旧“五段含故障”描述已按 `paper/main.tex` 更正为“五个正常段 + 独立冻结故障范围”。
- 2026-07-28：P2 最终双轴复审通过——规格轴确认首轮四项问题全部闭环；规范轴补齐 `FitAccessRecord` 公开类说明，并把估计段的笼统 `SCALER` 收窄为 `MONITORING_SCORE_SCALER`，明确它只表示监测分数尺度量，不授权模型输入预处理在估计段拟合。
- 2026-07-28：P3 新增独立 `PaperProtocolExperiment` 和严格配置解析——全体基线按 train、estimate、detection calibration、attribution calibration、frozen-normal replay 的全局顺序推进；PCA 同时输出 Hotelling `T^2` 与 SPE/Q，DAE 输出重构误差，一步 MLP 输出预测误差；checkpoint、监测尺度、静态 episode 分位、逐时刻 score/threshold/alarm/raw-index、校准 episode 和重放证据均保存为可审计产物。
- 2026-07-28：P3 双轴审查整改完成——校准基线逐个完整 episode 独立评分，禁止一步 MLP 跨 episode 配对；运行身份补齐 Git commit、数据/split/window/episode hash；development/frozen 模式和两类风险改为显式配置；DAE 必须显式使用正训练噪声；内存 DataLoader 适配器归还数据层；公开与关键内部接口补齐中文契约。规格和规范轴均无未解决高优先级问题。
- 2026-07-28：P4 新增严格过去 Attention--Koopman--T--S 正常模型——确定性位置编码保留历史顺序，``xi_k`` 单独进入锚点编码而 ``y_k`` 不进入；训练视野按显式种子在 ``1..N_max`` 均匀抽样，受保护历史只回填模型预测；T--S 规则、局部/组合算子、完整四组 Jacobian、自由多步损失、checkpoint 随机状态重放和三项 fail-closed 停止条件诊断均有公开测试。该证据只说明实现契约成立，不代表真实正常数据上的模型质量 gate 或论文故障性能已经通过。
- 2026-07-28：P5 新增纯 ``ProtectedMonitor.step``、延迟 anchor gate、hysteresis、重锚和 stale 生命周期、严格分离的 data/protected 双支路、不可变 ``MonitorState``/``MonitorTrace`` 与 ``StackedProtectedResidual``。受保护命令路径的数据结构不含 measurement；状态和 trace 绑定 gate 配置、模型配置与 checkpoint 参数联合 hash；五个正常阶段和独立冻结故障范围使用受控枚举。双轴审查后移除了无证据 coverage 声明入口，并补齐 trace 边界/mode/reset/identity 一致性门禁。该证据只证明 P5 信息边界和重放契约，不代表 P6 算子已认证或故障性能已评价。
- 2026-07-29：P6 新增受显式资源预算约束的 ``NominalJVPAssembler``，从共同路径上的完整 Jacobian/JVP 装配单位块下三角 ``G_nu``、anchor 传播 ``G_0``、输入/过程响应和命名传感器 JVP。解析 segment identity、在线 nominal 数值和认证状态严格分层；认证 provider 必须返回一个共享同一仿射系数向量的联合 ``OperatorEnclosure``，安全消费者只能通过联合 support 接口使用它。双轴审查后补齐严格布尔证据、分配前资源门禁、真实 JSON 产物往返和反序列化不变量。该证据不代表真实认证后端已存在，也不授权 P9 物理 singleton。

## 3. 当前判断

| 事项 | 当前判断 |
|---|---|
| 仓库性质 | 通用过程数据实验底座，不是论文方法的完整实现 |
| 当前 Koopman 能力 | P4 正常模型、P5 受保护在线参考和 P6 名义堆叠算子已实现；真实认证后端尚未实现 |
| 当前故障检测能力 | 已有基础正常阈值、FAR/MDR/FDR 和受保护参考，但没有动态阈值或结构化隔离 |
| 当前论文实施阶段 | P1-P6 已完成；当前进入 P7 white-space 堆叠残差后滤波 |
| 当前数据协议要求 | 五段正常数据：训练、估计、检测校准、归因校准、冻结正常测试；另有独立冻结故障测试 |
| 推荐主数据集 | 优先审计闭环 CSTR，最终选择仍需导师确认 |
| 第二数据集 | TTS 三容水箱优先，TE 可作为后续复杂过程扩展 |
| 论文数据边界 | 模型、超参数和阈值只能使用正常数据；故障数据只能用于冻结后的最终评价 |
| 当前实验结果 | 只有代码路径验证，不属于论文正式结果 |

## 4. 接下来要做

按优先级执行：

0. P0 已完成：实施计划、实验线说明、导航、工作记录和初学者沟通规则已进入阶段提交。P0 只冻结规则，没有实现算法。

1. P1 已完成并形成独立提交，不再改动其数据语义。

2. P2 已完成五段正常数据、隔离带、访问账本和冻结故障访问门禁的实现、相关验证与双轴复审，并形成独立提交。第五段是冻结正常测试；真实冻结故障测试是额外的独立访问范围，不能混写为正常第五段。

3. P3 已完成双轴审查整改并形成独立提交。

4. P4 已完成严格过去编码、T--S 局部 Koopman、自由多步 rollout、完整 Jacobian、训练损失和 fail-closed 诊断；P5 已完成受保护参考、监视器状态和堆叠残差；P6 已完成名义算子装配、联合认证协议、资源门禁和产物重放。

5. 当前按第 15 节以 TDD 实现 P7 white-space 堆叠残差后滤波、anchor 谱零化、guard 和 matched branches。

6. 只有通过全部阶段门、CSTR 许可核实并写入冻结 manifest 后，P10 才能执行一次性真实故障评价。

## 5. 需要导师确认

以《论文方法完整设计》第 24 节的 10 个问题为准（覆盖并细化了此前列表）：目标期刊、主/次数据集与许可、$\xi$ 建模、采样周期、动态阈值定位（确定性传播 + conformal 混合方案）、两处 critical 修正的认可、贡献砍序、故障可视化边界、硬件实验、慢漂移盲区表述。

## 6. 当前阻塞与风险

| 风险 | 状态 | 下一步 |
|---|---|---|
| CSTR 变量、故障时刻和协议元数据 | P1 已解决并测试 | P2 复用物理 schema，禁止重新猜列 |
| 创新是否与最近工作重合尚未确认 | 基本解决（收窄表述下成立） | 精读三个高威胁先行者，写 Related Work 对比 |
| 动态阈值的准确技术含义尚未冻结 | 设计已定（确定性传播 + conformal 混合） | 待导师确认（设计文档 §24 问题 5） |
| 老师稿两处 critical 修正未经导师确认 | 未解决 | 设计文档 §24 问题 6 |
| CSTR 数据许可 to_verify | 未解决 | 核实 MathWorks FE #66189 许可与引用 |
| 仓库没有完整论文方法实现 | 已知事实 | 按设计文档 §21 模块落点逐步开发 |
| 完整 199 个测试尚未全部运行 | 部分解决 | P6 专项 22 项及相关 58 项回归通过；全套测试留在 P10 最终闸门执行 |
| 当前理论未饱和 | 已知边界 | 先冻结稳定接口；认证 oracle 等未闭合能力保留未认证状态和拒识 |

## 7. 重要决策记录

| 日期 | 决策 | 原因 | 后续影响 |
|---|---|---|---|
| 2026-07-21 | 不直接跑大规模实验 | 数据边界、方法和评价协议尚未确认 | 先做仓库复现、数据审计和文献定位 |
| 2026-07-21 | 论文训练阶段坚持仅使用正常数据 | 这是老师稿最重要的研究限制 | 故障数据不得用于调参和阈值拟合 |
| 2026-07-21 | 合并技术架构和核心代码树 | 两份文档内容重复，维护成本较高 | 当时统一维护《技术架构与代码地图》；2026-07-28 后按内容变化频率重新拆分 |
| 2026-07-21 | 用户文档采用最小集合 | 避免大量空文档和重复信息 | 当前只维护导航、路线、架构地图和本记录 |
| 2026-07-26 | 方法类交付物用中文 LaTeX 并本地编译验证 | 数学内容 LaTeX 表达更合适，最终论文即 LaTeX；本机已装 MiKTeX | `.tex` 交付前必须 `pwsh docs\编译PDF.ps1` 通过 |
| 2026-07-26 | 采纳对老师稿的两处 critical 修正（待导师确认） | 原推论 1 不可实现；隔离配对错位 | 见设计文档 §11.3、§13.1 与 §24 问题 6 |
| 2026-07-26 | 新增 CLAUDE.md 作为 AI 协作入口 | 让后续 AI 会话快速掌握命令、边界与约定 | 该入口已于 2026-07-28 合并进 AGENTS.md，原文件删除 |
| 2026-07-26 | 按老师反馈重构三核心模块并直接产出英文 TFS 论文 | 老师判定设计核心内容不行，点名后滤波器/动态阈值/结构化隔离三处 | 设计文档第 10、14 节被取代；新增《论文核心重构与写作计划》与 MANUSCRIPT_CONTEXT.md；最终交付物改为 paper/main.tex（英文，实验数值占位） |
| 2026-07-28 | 统一代理入口并拆分架构与文件清单 | 工具专用规则重复，架构原理与文件计数的变化频率不同 | AGENTS.md 成为唯一代理入口；《技术架构》与《目录结构与文件数》分别维护 |
| 2026-07-28 | 论文代码按 P0-P11 分阶段实施并直接提交到 `main` | 论文尚未定稿，需要稳定接口和可回退的阶段边界；项目目前只有用户一人开发，不需要额外分支 | 每阶段 TDD、验证、双轴审查后自动创建独立 Git 提交 |
| 2026-07-28 | 冻结稳定接口，不把未完成理论写死为已认证实现 | 八轮理论审核仍有新增实质问题 | 先实现安全接口、线性特例和未认证路径；认证门单独验收 |
| 2026-07-28 | 新增独立论文编排层 | 通用 `Experiment.run()` 的 train/test 语义不能安全表达五阶段协议 | 论文流程组合现有底座，但不强改通用 Experiment |
| 2026-07-28 | CSTR 先验证，接口保持数据集无关 | CSTR 已审计且覆盖三类故障，后续仍需 TTS/TE 验证迁移性 | P1-P10 先完成 CSTR，P11 再扩展且不反调 CSTR |
| 2026-07-28 | 真实 CSTR 故障在协议冻结前只做 shape/onset/标签检查 | 防止故障测试反向影响模型、阈值和状态策略 | 开发期用正常数据和合成故障；正式故障性能只在 P10 读取 |

## 8. 关键验证记录

| 日期 | 验证内容 | 结果 | 能说明什么 | 不能说明什么 |
|---|---|---|---|---|
| 2026-07-21 | 核心配置测试 | 9 项通过 | 配置、注册和基础构建路径可用 | 不能证明完整仓库全部正确 |
| 2026-07-21 | 合成 CSTR 冒烟 | 成功 | 基础训练、阈值、指标和产物路径可运行 | 合成数值不是论文结果 |
| 2026-07-21 | 真实 CSTR preset 加载 | 成功 | 真实数据适配和 DataLoader 可用 | 示例模型不是论文基线 |
| 2026-07-21 | 完整测试套件 | 120 秒内未结束 | 需要分组检查耗时 | 不能据此判定测试失败 |
| 2026-07-26 | 设计文档 XeLaTeX 本地编译 | 通过，36 页，无错误/未定义引用 | LaTeX 源与本地工具链可用 | 不能说明数学内容全部正确 |
| 2026-07-26 | 老师稿 6 组定理/命题独立重推导 | 骨架成立；2 处 critical + 约 18 处 moderate 修正点 | 设计文档第 16 节清单可作论文写作依据 | 修正尚未经导师确认 |
| 2026-07-28 | pytest 收集 | 9 个测试文件、128 个测试可收集 | 当前测试发现和导入路径可用 | 不代表 128 个测试全部执行通过 |
| 2026-07-28 | 规划期相关回归测试 | 82 个测试中 76 通过、6 跳过 | 当前配置、数据、模型和评估相关选取路径未发现回归 | 不覆盖未来论文方法，也不等于全套通过 |
| 2026-07-28 | `python -m ruff check src tests examples` | 通过 | 项目源码、测试和示例满足当前 Ruff 规则 | 不包含 vendored skill 代码，也不证明运行行为正确 |
| 2026-07-28 | P1 CSTR 协议回归 | 相关 63 项中 57 通过、6 跳过；Ruff、导入和 CPU smoke 通过 | 逐行 onset、物理角色、追溯 hash、卡片路由和 sequence episode 边界可用 | 不代表 CSTR 许可已核实、五阶段切分已实现或故障性能已评价 |
| 2026-07-28 | P1 定向类型检查 | `real_process.py` 在 `--ignore-missing-imports` 下通过 | CSTR 适配器新增类型未发现错误 | DataModule 广泛定向检查仍受既有类型债和缺失 `pandas-stubs` 影响，不能声称全量 mypy 通过 |
| 2026-07-28 | P2 五阶段协议回归 | 相关 74 项中 68 通过、6 跳过；P2 模块定向 mypy、Ruff、导入和 `git diff --check` 通过 | 五段比例、隔离带、窗口/episode 不重叠、不可绕过的访问账本、分位冻结顺序、来源 hash 和冻结故障门禁可用 | 不代表全套 145 项测试已运行、CSTR 许可已核实或任何故障性能已评价 |
| 2026-07-28 | P3 论文协议与正常基线 | P3 定向 10 项及训练、checkpoint、P2 协议和示例相关 40 项回归全部通过；三个相关模块定向 mypy、Ruff、安静导入和差异检查通过 | 三类基线可在 CPU、无故障数据条件下完成五阶段正常闭环；校准 episode 独立；静态阈值不会冒充论文动态阈值；checkpoint、Git/data/split/window/episode 身份可重放追溯 | 不代表全套 155 项测试已运行、论文 P4-P9 方法已实现或任何故障性能已评价 |
| 2026-07-28 | P4 严格过去正常模型 | P4 专项 12 项、相关回归、Ruff、定向 mypy、安静导入和差异检查通过 | 历史顺序/当前工况、目标隔离、均匀视野、T--S 退化、四组完整 Jacobian、梯度、CPU 训练、checkpoint/mask/horizon 状态重放和停止条件诊断契约成立 | 不代表全套 167 项已运行、真实正常数据质量 gate 已通过、P5-P9 已实现或任何故障性能已评价 |
| 2026-07-28 | P5 受保护参考与堆叠残差 | P5 专项 10 项及 P4、论文协议、既有工作流和训练损失相关 72 项回归通过；Ruff、定向 mypy、安静导入、177 项收集和差异检查通过 | 延迟候选、hysteresis、重锚/stale、固定命令直接非污染、受控阶段、联合 monitor 身份、trace 重放和残差窗口边界契约成立 | 不代表全套 177 项已运行、anchor 一定正常、P6 算子已认证、P7-P9 已实现或任何故障性能已评价 |
| 2026-07-29 | P6 堆叠算子与联合认证状态 | P6 专项 22 项及 P5、P4 Jacobian、论文协议相关 58 项回归通过；Ruff、定向 mypy、安静导入、199 项收集和差异检查通过 | 手算/非线性 exact propagation、``G_nu/G_0`` 索引、输入/过程/传感器响应、名义/不可用/认证边界、共享仿射不确定元素、严格证据类型、资源预算和 JSON 产物重放契约成立 | 不代表全套 199 项已运行、真实认证后端已实现、P7--P9 已完成或任何故障性能已评价 |

## 9. 每周更新模板

后续每周只在本文件末尾追加一条简短记录：

### 日期

- 本周要回答的问题：
- 完成的工作：
- 最关键的证据：
- 结果支持什么：
- 结果不支持什么：
- 新的决定：
- 当前阻塞：
- 下周第一优先级：

不要记录每个 epoch 或每次调试命令。原始配置、日志、checkpoint 和逐时刻指标应保存在运行产物中，本文件只保留能够改变研究判断的信息。

### 2026-07-26

- 本周要回答的问题：任务书的 14 项设计任务能否闭合成一份可实现、可验证的完整方法设计？老师稿的理论是否全部站得住？
- 完成的工作：交付《论文方法完整设计》（24 节 LaTeX，本地编译 36 页）；6 组定理独立重推导；4 条文献检索线；仓库与闭环 CSTR 数据深度审计；装好本地 LaTeX 工具链；曾新增 CLAUDE.md（2026-07-28 已并入 AGENTS.md）并整理文档。
- 最关键的证据：推论 1 按老师稿原文不可实现（校准分数不可观测）；隔离层残差—响应矩阵配对错位——两处均已给出修正方案。
- 结果支持什么：三项贡献在收窄表述下成立；数学骨架整体正确；闭环 CSTR 的 8 类故障恰好覆盖过程/执行器/传感器三分结构。
- 结果不支持什么：不能声称首创"参考保护"思想（Kloft–Laskov、Wang 2005、US7587296、Zhang 2002 均为先行者）；修正未经导师确认前不能写入论文。
- 新的决定：方法类交付物中文 LaTeX + 本地编译验证；采纳两处 critical 修正（待导师确认）。
- 当前阻塞：导师确认（设计文档第 24 节 10 个问题）；数据许可 to_verify。
- 下周第一优先级（当时计划，已于 2026-07-27 作废）：与导师过设计文档；启动数据前置修补与 `FourWayNormalSplitter`。

### 2026-07-26（老师反馈后追加）

- 本周要回答的问题：老师判定核心内容不行——三处（堆叠残差后滤波器、输入/工况动态阈值、三类故障结构化隔离）到底弱在哪，怎么重构？
- 完成的工作：三模块第一性原理诊断；制定《论文核心重构与写作计划》（统一框架：一个堆叠残差对象 + 扰动/签名三组算子）；创建 MANUSCRIPT_CONTEXT.md；冻结英文 TFS 论文写作路线（P1 推导 → P2 对抗性审查 → P3 全文 → P4 编译审计）。
- 最关键的证据：设计文档第 14 节把后滤波器论证掉了而非设计出来；$\gamma^{\mathrm{det}}$ 是标量球递推、输入实测值不进入界；过程故障类只是"正交剩余"而非被设计的类——老师三点批评均有确切病灶。
- 结果支持什么：三个突破点可以在现有定理 1/Jacobian/锚点框架上统一重构，不必推翻底层。
- 结果不支持什么：不能声称现设计的检测/隔离评价层已达可投稿水平；实验数值在实现完成前只能占位。
- 新的决定：见第 7 节决策表 2026-07-26 最后一行。
- 当前阻塞：计划待用户/导师确认后执行 P1。
- 下周第一优先级：执行 P1 三模块完整推导与数值抽查。

### 2026-07-26（P1–P4 执行完成）

- 本周要回答的问题：重构方案能否经受对抗性审查并落成完整英文论文？
- 完成的工作：P1 三份推导工单 + 14/14 数值抽查（`.scratch/重构推导-*.md`）；P2 六个后台 agent（3 文献边界 + 3 反驳审查）；P3/P4 `paper/main.tex` + `refs.bib` 完成并编译通过（IEEEtran 11 页，0 错误/0 未定义引用/0 超宽行，逐页目检）。
- 最关键的证据：反驳审查发现并修复了四个实质问题——零化器精确吞掉锚点后首步故障（加守卫支路）、过程类子空间结构性为空（改持续子空间+能量检验）、校准分段泄漏（改四分块）、上下文抵消引理（$\delta^\chi$ 不可作隔离特征）。
- 结果支持什么：三大核心模块现在各有定理支撑（2 中心定理 + 7 命题 + 1 引理 + 1 推论），文献边界核实后的收窄表述成立；数学骨架经数值验证。
- 结果不支持什么：实验表格仍为占位——在方法实现与冻结评估完成前，论文不能投稿；三个最威胁先行者（Irani CEP 2024、Ma TFS 2023、Taheri 2026）已引用但对比实验未做。
- 新的决定：见 `MANUSCRIPT_CONTEXT.md` §6.1 定稿设计决策表（10 项，全部来自反驳审查）。
- 当前阻塞：导师审阅论文稿；方法实现（设计文档 §21 落点）。
- 下周第一优先级（当时计划，已于 2026-07-27 作废）：导师过稿；启动 `FourWayNormalSplitter` 与方法模块实现。

### 2026-07-27（三核心模块八轮理论深化）

- 本周要回答的问题：后滤波器、输入/运行状态动态阈值、执行器/传感器/过程结构化隔离能否推导到同模型连续两轮再无实质反例？
- 完成的工作：恢复其他 AI 的深化断点；建立 white-space anchor quotient、有限 episode 阈值、full-state/raw-record explanation graph 与支持轨迹理论；完成八轮同基础模型对抗审核；把 R1--R99 的有效修正写回 `paper/main.tex`、四份理论终稿、八轮账本和最终裁定矩阵。
- 最关键的证据：第七轮仍新增有限样本分位、校准循环、零阈值、full-normal 归因等 R89--R96；第八轮仍新增真支持轨迹、两阶段独立校准和 Gram 阈值平方量纲 R97--R99。
- 结果支持什么：三个模块现在共享同一部署算子/阈值几何和完整 explanation graph；误报、检测功效、隔离 soundness 与物理可识别性已经分账；所有单标签结论都带有限 episode、完整观测与轨迹级覆盖条件。
- 结果不支持什么：八轮内没有取得连续两轮 `NO_NEW_MATERIAL_CONCLUSION`，因此不能宣称理论饱和或全球首创；正式实验、物理 signature coverage、概率预算和运行时 oracle 均未验证。
- 新的决定：按预先冻结的最多八轮规则标记 `Pause / Not theoretically saturated`；正常数据协议由四分改为五段（训练/估计/检测校准/归因校准/冻结测试）；后续实现不能继续沿用 `FourWayNormalSplitter`。
- 当前验证：`paper/main.tex` 花括号 1451/1451、环境栈、78 个标签/87 个引用、55 次 citation/48 个 BibTeX key 均一致，`git diff --check` 通过。`latexmk` 与 `chktex` 均被 MiKTeX fresh installation 阻断，当前 PDF/超宽行目检未完成。
- 当前阻塞：完成 MiKTeX 首次 setup/updates；实现 certified segment-integral enclosure、full-normal outer oracle、monotone refinement cache 与五段切分；导师确认理论降级边界。
- 下周第一优先级：先完成 MiKTeX setup 并重新编译/逐页目检，再把 `FourWayNormalSplitter` 规格升级为 FiveStageNormalSplitter；在没有外部物理证据前保持 signature 风险为 coverage-conditional。

### 2026-07-28（P0 实施规划确认）

- 本周要回答的问题：论文尚未定稿时，怎样开始写代码又不把未闭合理论、旧四段协议或故障测试泄漏写死？
- 完成的工作：全面核对代码库和当前论文协议；形成 P0-P11 代码实施计划；单独编写面向初学者的实验线说明；确认直接在 `main` 分阶段提交。
- 最关键的证据：当前通用 `Experiment` 和 `DataModule` 不能安全表达五阶段协议；CSTR 适配器仍有整段故障标签和 `u/y` 角色缺失；当前论文要求检测与归因使用独立 calibration episode。
- 结果支持什么：可以先冻结稳定接口，按 CSTR 首验、数据集无关接口、TDD 和阶段门逐步开发。
- 结果不支持什么：不能声称论文方法已实现、理论已饱和、正式故障评价已完成或 CSTR 数据许可已通过。
- 新的决定：P0-P11 每阶段测试、审查后在 `main` 自动提交；真实故障性能只在 P10 冻结协议后读取。
- 当前阻塞：P1 的 onset 权威来源、变量顺序和许可需要在实现前复核；当前等待用户明确开始 P1。
- 下周第一优先级：P0 提交后等待用户明确说“开始 P1”，不提前修改算法代码。

### 2026-07-28（P1 CSTR 协议元数据修正）

- 本周要回答的问题：闭环 CSTR 的物理列、onset、episode 和原始来源能否通过公开数据入口准确追溯？
- 完成的工作：修正 7 变量物理 schema、逐行 onset 标签、故障族和文件 hash；为 DataModule 增加独立 test segment 传播；同步 dataset card。
- 最关键的证据：normal 为 16814 行；fault01--08 各 1201 行且分别为 200 行正常、1001 行对应故障；sequence test 保留 8 个 episode id。
- 结果支持什么：P2 可以从具有正确物理语义和 episode 边界的 CSTR 数据开始五阶段正常切分。
- 结果不支持什么：不能声称许可已核实、正式故障评价已运行或论文方法已实现。
- 新的决定：后续阶段直接以实施计划为规格，不再创建阶段规格文件或等待重复确认。
- 当前阻塞：CSTR 许可仍为 `to_verify`；不阻塞 P2-P9 本地开发，但阻塞 P10 正式冻结结果。
- 下周第一优先级：实现 P2 五阶段正常数据、隔离带和访问账本。

### 2026-07-28（P2 五阶段正常数据协议）

- 本周要回答的问题：怎样把一条正常长序列安全地分成五个用途互斥阶段，并让真实故障测试在冻结前不可访问？
- 完成的工作：新增五阶段切分、保守隔离带、合法窗口与完整 episode、确定性 manifest、拟合访问账本、CanonicalDataset 入口和冻结故障许可门禁；补充 9 个公开行为测试。
- 最关键的证据：P2 相关 74 项中 68 项通过、6 项跳过；P2 模块定向 mypy、Ruff、安静导入和差异检查通过。
- 结果支持什么：模型、估计、两类校准和冻结正常测试现在有可审计的数据边界；故障数组的变化不会改变正常训练与估计阶段 hash。
- 结果不支持什么：不能据此声称论文模型、动态阈值、结构化隔离或正式故障评价已经实现。
- 新的决定：故障测试仍作为五段之外的独立封存范围；协议 hash、冻结状态和 `verified` 许可三者缺一不可。
- 当前阻塞：CSTR 许可仍为 `to_verify`，继续阻塞 P10 正式结果；不阻塞 P3-P9 的正常数据开发。
- 下周第一优先级：按第 12 节完成 P4 严格过去的 Attention--Koopman--T--S 正常模型及三方 Jacobian 验证。
